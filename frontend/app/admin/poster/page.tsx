import PosterDesigner from "../../components/PosterDesigner";

// Drag-and-drop poster template editor. Edits the composite template.json
// (text + logos + background) that every generated poster is built from.
export default function PosterDesignerPage() {
  return <PosterDesigner />;
}
