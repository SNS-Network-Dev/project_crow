import CheckinKiosk from "../../components/CheckinKiosk";

// Phone or iPad kiosk — one unified, full-screen, auto-capture check-in surface.
export default function CheckinPage() {
  return <CheckinKiosk />;
}
