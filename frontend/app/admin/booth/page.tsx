import BoothControl from "../../components/BoothControl";

// Operator control surface for the photo booth (live status + variants + prompts).
export default function BoothControlPage() {
  return <BoothControl />;
}
