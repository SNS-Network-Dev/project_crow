import CheckinKiosk from "../components/CheckinKiosk";

// Same unified auto-capture check-in as /admin/checkin (kept as an alias route).
export default function KioskPage() {
  return <CheckinKiosk />;
}
